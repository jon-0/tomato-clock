// sync user preference of timer with displayed timer
var timer = document.getElementById("timer");
var timerLength = document.getElementById("timerLength");

timer.innerHTML = timerLength.innerHTML;

// add functionality to plus and minus buttons

// add timer/countdown function

// start "rest" timer when countdown finishes